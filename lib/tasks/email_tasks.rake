desc 'send morning email'
task send_morning_email: :environment do
    puts 'this works'
#    @users = User.all
#    @users.each do |user|
#        MusicMailer.morning_email(user).deliver!
#    end
end